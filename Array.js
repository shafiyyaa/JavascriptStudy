// Tentukan total jumlah dari array berikut:
// Var scores = [1,2,3,4,5];

var scores = [1, 2, 3, 4, 5];
var total = 0;

for (var i = 0; i < scores.length; i++) {
    total += scores[i];
}

console.log(total);